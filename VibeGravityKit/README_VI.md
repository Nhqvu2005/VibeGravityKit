# 🌌 VibeGravityKit

> **Công Ty Phần Mềm AI-Native Trong Một Hộp.**
> *Xây dựng phần mềm cấp doanh nghiệp với đội ngũ 18 Agent AI — **giao việc song song** để đạt tốc độ tối đa và tiết kiệm tối đa chi phí token.*

---

## 🎩 VibeGravityKit là gì?

Hãy tưởng tượng bạn có một đội **kỹ sư full-stack** ngay trong IDE.
**VibeGravityKit** biến IDE của bạn thành một đội phối hợp gồm **18 agent chuyên biệt**, từ **Kiến trúc sư** thiết kế database cho bạn, đến **Nhà nghiên cứu** tìm kiếm web bằng DuckDuckGo.

Nhưng đây là tính năng sát thủ: **Chúng tôi ghét lãng phí token.**
- **Context Manager**: Nén code trước khi AI đọc. (Tiết kiệm ~50% token).
- **Context Router**: Chỉ truy vấn dữ liệu liên quan từ 34+ nguồn. (Tiết kiệm ~70% token).
- **Diff Applier**: Áp dụng bản vá chính xác thay vì viết lại file. (Tiết kiệm ~90% token).

---

## 🚀 Cách Hoạt Động — Hai Cách Để Xây Dựng

VibeGravityKit cung cấp **hai chế độ làm việc mạnh mẽ** để bạn xây dựng phần mềm cùng đội AI:

### Chế độ 1: `@[/quickstart]` — Mì Gói 🍜
> **Mô tả ý tưởng → Nhận sản phẩm hoạt động.** Nhanh, tự động, ngon lành.
> Như mì gói — chỉ cần thêm nước (ý tưởng của bạn) là ăn được.

```
Bạn → Leader xác nhận kế hoạch → Tự động xây dựng → ♻️ Kiểm tra → Triển khai → Xong!
```

**Phù hợp cho:** MVP, prototype, demo, hackathon, hoặc người dùng không chuyên kỹ thuật chỉ muốn kết quả.

**Cách hoạt động:**
1. Mô tả ý tưởng — mơ hồ cũng được ("Tôi muốn một cửa hàng online").
2. Leader tự phát hiện tech stack + kiểm tra kho template (tiết kiệm ~70% token nếu trùng).
3. **Leader cho bạn xem checklist đơn giản** — bạn duyệt/sửa trước khi xây.
4. Quá trình xây dựng chạy **hoàn toàn tự động** với các agent song song.
5. **♻️ Vòng kiểm tra (tối đa 5)**: Leader quét codebase để xác minh MỌI tính năng trong checklist thực sự hoạt động. Thiếu hoặc hỏng? → gửi agent phụ sửa → lặp đến khi tất cả ✅.
6. **Tự động triển khai** qua Cloudflare Tunnel → bạn nhận link trực tiếp ngay.

**Ví dụ:**
```
Bạn: "Xây cho tôi một trình rút gọn URL đơn giản"
Quickstart:
  📋 Kế hoạch: [Trang chủ ✓] [Rút gọn URL ✓] [Chuyển hướng ✓] [Copy link ✓]
  "Muốn thêm hoặc bớt gì không?"
Bạn: "Được rồi ✅"
Quickstart:
  🔥 Thiết kế... → 💻 Xây dựng... → ♻️ Kiểm tra tính năng (2/5)...
  🚀 Xong! https://xxx.trycloudflare.com
```

---

### Chế độ 2: `@[/leader]` — Kho Báu Chậm Rãi 🍲
> **Bạn là Đầu Bếp. Leader là phụ bếp.** Kiểm soát toàn bộ ở mọi bước.
> Như nấu món hầm — mất thời gian, nhưng kết quả đạt chất lượng production.

```
Bạn → Leader → Các Agent → Báo cáo từng giai đoạn → Bạn duyệt → Giai đoạn tiếp
```

**Phù hợp cho:** Ứng dụng production, dự án doanh nghiệp, hoặc khi chất lượng quan trọng hơn tốc độ.

**Cách hoạt động:**
1. Nói cho Leader bạn muốn xây gì.
2. Leader phân tích, brainstorm, và trình bày kế hoạch.
3. **Bạn duyệt kế hoạch** ✅
4. Leader **tự động giao việc** cho đúng agent:

| Giai đoạn | Agent | Việc gì | Chế độ |
|-----------|-------|---------|--------|
| 📋 Lập kế hoạch | `@[/planner]` | PRD, user stories, timeline | Tuần tự |
| 🏗️ Kiến trúc + 🎨 Thiết kế | `@[/architect]` + `@[/designer]` | Schema DB + UI/UX | ⚡ **SONG SONG** |
| 💻 Phát triển | `@[/frontend-dev]` + `@[/backend-dev]` | Xây frontend + backend đồng thời | ⚡ **SONG SONG** |
| 🧪 QA & Sửa lỗi | `@[/qa-engineer]` | Test → Tìm bug → Sửa → Test lại | Tuần tự |
| 🚀 Ra mắt | `@[/devops]` + `@[/security]` + `@[/seo]` + `@[/docs]` | Deploy, audit, SEO, docs — tất cả cùng lúc | ⚡ **SONG SONG** |

5. **Sau mỗi giai đoạn**, Leader báo cáo kết quả và chờ bạn duyệt.
6. **⚡ Giao việc song song**: Kiến trúc + Thiết kế chạy cùng lúc. Frontend + Backend chạy cùng lúc. **Nhanh hơn tới 4 lần.**
7. **QA Vòng thông minh**: Nếu bug không sửa được, Leader gọi `@[/meta-thinker]` + `@[/planner]` để suy nghĩ lại. Tối đa **3 lần thử lại**.

---

### So sánh chế độ
| | 🍜 `@[/quickstart]` | 🍲 `@[/leader]` |
|---|---|---|
| **Triết lý** | Mì gói — nhanh & dễ | Món hầm — cẩn thận & kỹ |
| **Sự tham gia** | Duyệt kế hoạch 1 lần | Duyệt từng giai đoạn |
| **Agent song song** | ⚡ Có | ⚡ Có |
| **Kiểm tra tự động** | ♻️ Tự lặp (tối đa 5) | Thủ công từng giai đoạn |
| **Tự triển khai** | ✅ Cloudflare Tunnel | Thủ công |
| **Template-first** | ✅ Tự phát hiện | Thủ công |
| **Phù hợp cho** | MVP, demo, người mới | Ứng dụng production, dự án quan trọng |

---

## 🧬 Team Profiles — Mang Style Code Của Bạn Qua Các Dự Án (v2.9.0)

> **Vấn đề:** Mỗi `vibegravity init` bắt đầu từ đầu — agent quên style code, sở thích tech, và các bug fix trước đó.
> **Giải pháp:** Team profile liên tục **tự học từ bạn** khi bạn làm việc, và mang kiến thức đó tới mọi dự án mới.

### Bắt Đầu Nhanh
```bash
# Bước 1: Tạo team rỗng
vibegravity team create my-team

# Bước 2: Khởi tạo dự án với team đó
vibegravity init antigravity --team my-team

# Bước 3: Làm việc bình thường với @[/leader] hoặc @[/quickstart]
# → Agent TỰ HỌC style code của bạn mỗi khi bạn:
#    ✅ Xác nhận kế hoạch  → code được quét, DNA cập nhật
#    ✅ Hoàn thành giai đoạn → chỉ thị trở thành quy tắc
#    ✅ Sửa bug             → bài journal đồng bộ về team
```

**Chỉ có vậy.** Không cần file cấu hình, không cần thiết lập thủ công. Team tự học thụ động.

### Cơ Chế Tự Học

**Leader/Quickstart** đóng vai trò quan sát. Tại mỗi điểm kích hoạt, nó gọi `team_learner.py`:

| Kích hoạt | Việc gì xảy ra | Lệnh Agent chạy |
|-----------|----------------|-----------------|
| 🔵 **Kế hoạch được duyệt** | Quét mã nguồn → phát hiện stack, naming style, kiến trúc → tạo/cập nhật Team DNA | `team_learner.py --scan-project .` |
| 🟢 **Giai đoạn hoàn thành** | Leader ghi nhận chỉ thị của bạn (ví dụ "viết bằng tiếng Anh") → lưu thành quy tắc | `team_learner.py --directive "write in English"` |
| 🔴 **Bug được sửa** | Bài journal tự đồng bộ về team profile | `team_manager.py save-back` |
| 🟡 **Quét thủ công** | Bạn quét codebase có sẵn (tùy chọn) | `vibegravity team scan my-team --path ./project` |
| 🟣 **Học thủ công** | Yêu cầu team học từ dự án hiện tại | `vibegravity team learn` |

### Team DNA — Style Của Bạn Trong Một Dòng
```
naming:camelCase|comments:minimal|lang:typescript+python|fe:react|be:fastapi|css:tailwind|arch:feature|indent:2
```

Định dạng nhỏ gọn này (~50 token) cho mọi agent biết chính xác bạn thích code như thế nào. Nó tự lớn lên khi bạn làm việc.

### Code Scanner Phát Hiện Gì
| Danh mục | Phát hiện từ |
|----------|-------------|
| Tech stack | `package.json`, `requirements.txt`, `tsconfig.json` |
| Quy ước đặt tên | Phân tích regex: camelCase vs snake_case |
| Mật độ comment | Tỷ lệ dòng comment / tổng dòng |
| Xử lý lỗi | Tần suất try/catch mỗi hàm |
| Kiến trúc | Cấu trúc thư mục: feature-based vs layer-based |
| Dấu ngoặc, chấm phẩy, thụt lề | Phân tích file nguồn |

### Hệ Thống Bộ Nhớ 3 Tầng
| Tầng | Lưu gì | Chi phí token | Khi nào tải |
|------|--------|--------------|-------------|
| 🔴 **Hot** | Team DNA (1 dòng) + quy tắc top | ~50 token | Luôn luôn — mọi phiên |
| 🟡 **Warm** | Toàn bộ quy tắc + index journal | ~200 token | Theo yêu cầu — tìm TF-IDF |
| 🔵 **Cold** | Bài lưu trữ + lịch sử DNA | 0 token | Chỉ khi bạn yêu cầu |

### Chống Trùng Lặp Quy Tắc

Khi thêm chỉ thị (thủ công hoặc từ leader), hệ thống kiểm tra **quy tắc tương tự đã tồn tại chưa**:

1. **Chuẩn hóa** — bỏ từ đệm ("please", "always", "must", "should"...)
2. **Rút gốc** — giảm hậu tố: "documentation" → "document", "writing" → "writ"
3. **Mở rộng viết tắt** — "docs" → "document", "ts" → "typescript"
4. **Độ tương tự Jaccard** — so sánh trùng lặp token (ngưỡng ≥ 50%)
5. **Nếu trùng** → tăng `frequency` thay vì tạo bản sao
6. **Nếu frequency ≥ 3** → tự động thăng lên tầng Hot (tải mọi phiên)

```
Ví dụ:
  Quy tắc có sẵn: "write docs in English"           (freq: 2)
  Chỉ thị mới:     "always write documentation in English"
  → Chuẩn hóa:     "write document english" = "write document english"
  → Tương tự:      1.0 (khớp hoàn toàn sau chuẩn hóa)
  → Kết quả:       frequency → 3, tự thăng lên Hot 🔥
```

### Lệnh CLI Quản Lý Team
```bash
# Quản lý team
vibegravity team create <name>                # Tạo team rỗng (tự học khi bạn làm việc)
vibegravity team list                         # Liệt kê tất cả team
vibegravity team show <name>                  # Xem DNA + thống kê
vibegravity team delete <name>                # Xóa team

# Học
vibegravity team scan <name> --path <project> # Quét code vào team (tùy chọn)
vibegravity team learn                        # Quét style code dự án hiện tại
vibegravity team learn --directive "text"     # Thêm chỉ thị cụ thể thành quy tắc

# Quy tắc — dạy team rõ ràng
vibegravity team rule add "Luôn viết tài liệu bằng tiếng Anh"
vibegravity team rule add "Dùng Tailwind" --agent frontend-dev
vibegravity team rule list
vibegravity team rule remove <id>

# Chia sẻ kiến thức
vibegravity team sync <other-team>            # Gộp kiến thức team khác
vibegravity team export <name>                # Chia sẻ dạng .zip
vibegravity team import team-file.zip         # Import từ .zip
```

---

## ⚙️ Cài Đặt

### Cài Đặt Toàn Cục (Khuyến Nghị)

```bash
git clone https://github.com/Nhqvu2005/VibeGravityKit.git
cd VibeGravityKit
pip install .
```
*(Yêu cầu Python 3.9+ & Node.js 18+)*

**Sử dụng trong dự án mới:**
```bash
cd my-project
vibegravity init                              # Cài đặt cho TẤT CẢ IDE
vibegravity init antigravity --team my-team   # Cài + inject team profile
```

## 🛠️ Lệnh CLI
| Lệnh | Mô tả |
|-------|-------|
| `vibegravity init [ide] [--team name]` | Cài cho tất cả/IDE cụ thể, tùy chọn với team profile |
| `vibegravity list` | Liệt kê tất cả 18 agent chuyên biệt |
| `vibegravity doctor` | Kiểm tra sức khỏe môi trường (Python, Node, Git) |
| `vibegravity update` | Tự cập nhật phiên bản mới nhất |
| `vibegravity version` | Hiện phiên bản hiện tại |
| `vibegravity team create <name>` | Tạo team rỗng (tự học khi làm việc) |
| `vibegravity team scan <name> --path <dir>` | Quét code dự án vào team (tùy chọn) |
| `vibegravity team learn` | Quét conversation logs → trích xuất thói quen |
| `vibegravity team list / show / delete` | Quản lý team profile |
| `vibegravity team rule add/list/remove` | Quản lý quy tắc team |
| `vibegravity team sync <other-team>` | Gộp kiến thức team khác |
| `vibegravity team export / import` | Chia sẻ team dạng .zip |
| `vibegravity brain` | Quản lý project brain |
| `vibegravity journal` | Knowledge journal |

## 🌐 Hỗ Trợ Đa IDE

VibeGravityKit hoạt động native với **4 AI IDE**:

| IDE | Lệnh | Tạo ra |
|-----|-------|--------|
| **Antigravity** | `vibegravity init antigravity` | `.agent/` (workflows + skills) |
| **Cursor** | `vibegravity init cursor` | `.cursor/rules/*.mdc` |
| **Windsurf** | `vibegravity init windsurf` | `.windsurf/rules/*.md` |
| **Cline** | `vibegravity init cline` | `.clinerules/*.md` |

```bash
# Ví dụ: Cài đặt cho Cursor IDE
cd my-project
vibegravity init cursor
# → 18 quy tắc agent được cài vào .cursor/rules/
```

---

## 🎮 18 Agent (Ví Dụ Sử Dụng)

Trong VibeGravityKit, **Bạn là Ông Chủ.** Chỉ cần chat với agent bằng `@`.

### 1. Đội Chiến Lược & Tầm Nhìn 🧠

**@[/leader]** (Cánh Tay Phải Của Ông Chủ)
> "Tôi muốn xây một bản sao Spotify. Điều phối toàn bộ kế hoạch."
*(Điều phối tất cả agent, báo cáo từng giai đoạn, quản lý vòng QA)*

**@[/quickstart]** (Chế Độ Tự Động)
> "Xây cho tôi ứng dụng quản lý công việc với React và Express."
*(Chạy mọi thứ end-to-end, giao dự án hoàn chỉnh kèm báo cáo)*

**@[/meta-thinker]** (Cố Vấn Sáng Tạo)
> "Tôi muốn xây ứng dụng giao đồ ăn nhưng phải độc đáo. Brainstorm ý tưởng."
*(Tạo: `vision_brief.md` với xu hướng, đối thủ, và góc nhìn độc đáo)*

**@[/planner]** (Quản Lý Dự Án)
> "Chia tính năng 'Hồ Sơ Người Dùng' thành 5 user stories với tiêu chí chấp nhận."
*(Tạo: `user-stories.md`)*

**@[/researcher]** (Nhà Nghiên Cứu Web)
> "Tìm kiếm xu hướng AI SaaS 2025 và đối thủ cạnh tranh hàng đầu."
*(Dùng **DuckDuckGo API** — không phụ thuộc, chỉ dùng stdlib. Chế độ `--compact` cho output tiết kiệm token)*

**@[/tech-stack-advisor]** (CTO)
> "Đề xuất tech stack cho bot giao dịch tần suất cao bằng Python."
*(Tạo: `tech-stack.md`)*

### 2. Đội Thiết Kế & Sản Phẩm 🎨

**@[/architect]** (Kiến Trúc Sư Hệ Thống)
> "Thiết kế schema Prisma cho SaaS multi-tenant với thanh toán subscription."
*(Tạo: `schema.prisma`)*

**@[/designer]** (Chuyên Gia UI/UX)
> "Tạo bảng màu dark-mode tối ưu và config Tailwind cho dashboard crypto."
*(Tạo: `tailwind.config.js`)*

**@[/mobile-dev]** (Trưởng Nhóm Mobile)
> "Scaffold dự án Expo Router mới với TypeScript và NativeWind."
*(Chạy: `npx create-expo-app`)*

### 3. Đội Kỹ Thuật 💻

**@[/frontend-dev]** (Lập Trình Viên Web)
> "Triển khai nút 'Đăng nhập Google' bằng NextAuth.js."
*(Cập nhật: `src/components/Login.tsx` dùng `diff-applier`)*

**@[/backend-dev]** (Lập Trình Viên API)
> "Tạo endpoint POST /api/orders với validate input bằng Zod."
*(Cập nhật: `src/app/api/route.ts`)*

**@[/devops]** (Kỹ Sư Hạ Tầng)
> "Tạo Dockerfile và docker-compose.yml cho ứng dụng Next.js + Postgres."
*(Tạo: `Dockerfile`, `docker-compose.yml`)*

### 4. Đội Chất Lượng & Hỗ Trợ 🛡️

**@[/knowledge-guide]** (Người Giải Thích Code)
> "Giải thích luồng xác thực trong dự án này hoạt động như thế nào."
*(Giải thích code & ghi ý tưởng cải tiến vào `.agent/memory/ideas_inbox.md`)*

**@[/qa-engineer]** (Tester)
> "Tạo unit test cho hàm `calculateTax` trong `utils.ts`."
*(Tạo: `tests/utils.test.ts`)*

**@[/security-engineer]** (Chuyên Viên Bảo Mật)
> "Quét dự án tìm secrets hardcode và lỗ hổng OWASP."
*(Chạy: `vuln_scan.py`)*

**@[/tech-writer]** (Chuyên Gia Tài Liệu)
> "Viết RELEASE_NOTES.md cho phiên bản 1.0 giải thích tính năng mới."
*(Tạo: `RELEASE_NOTES.md`)*

**@[/seo-specialist]** (Chuyên Gia SEO)
> "Kiểm tra `index.html` xem thiếu meta tag và open graph data gì."
*(Chạy: `seo_check.py`)*

**@[/code-reviewer]** (Chất Lượng Code)
> "Quét thư mục src/ tìm anti-pattern, vấn đề bảo mật, và lỗi đặt tên."
*(Chạy: `reviewer.py` → Điểm Chất Lượng A-F)*

**@[/release-manager]** (Kỹ Sư Phát Hành)
> "Tạo changelog từ v2.0.0 và nâng phiên bản lên 2.6.0."
*(Chạy: `release.py` → CHANGELOG.md + nâng version)*

---

## 📂 Cấu Trúc Dự Án
```bash
.agent/
├── workflows/       # "Bộ Não": Hướng dẫn cho từng vai trò
├── skills/          # "Đôi Tay": Script Python thực hiện công việc
└── brain/           # Ngữ cảnh & Bộ nhớ dự án
```

## 🧰 Smart Context Protocol

Thay vì agent đọc toàn bộ file dữ liệu (hàng trăm dòng), họ truy vấn **Context Router** để lấy đúng thứ cần:

```bash
# Tìm kiếm trên TẤT CẢ 34 nguồn dữ liệu:
python .agent/skills/context-router/scripts/context_router.py --query "fintech"
# → Chỉ trả về kết quả khớp (tiết kiệm ~70% token)

# Tìm trong một skill cụ thể:
python .agent/skills/context-router/scripts/context_router.py --skill meta-thinker --query "SCAMPER"

# Liệt kê tất cả dữ liệu có sẵn:
python .agent/skills/context-router/scripts/context_router.py --list
```

## 📦 Kho Template

Bắt đầu dự án mới ngay lập tức với template có sẵn:

```bash
# Duyệt 7 template:
python .agent/skills/template-marketplace/scripts/template_engine.py --action list

# Xem chi tiết template:
python .agent/skills/template-marketplace/scripts/template_engine.py --action show --template saas

# Có sẵn: saas, ecommerce, blog, api, landing, dashboard, mobile
```

---

## 📋 Nhật Ký Thay Đổi

### v2.9.0
- 🧬 **Team Profiles** — Style code, quy tắc, và kiến thức liên tục qua các dự án
  - `team create`, `team scan`, `team learn`, `team inject`, `team sync`, `team export/import`
  - **Code Scanner** — tự phát hiện stack, naming, kiến trúc từ file nguồn
  - **Team DNA** — profile nhỏ gọn 1 dòng (~50 token)
  - **Bộ nhớ 3 tầng** (Hot/Warm/Cold) với tìm kiếm TF-IDF
  - **Tự học** — leader quét code + ghi chỉ thị tại các điểm plan/phase
  - **Chống trùng quy tắc** — Jaccard + stemming, tự thăng khi freq ≥ 3
  - `init --team <name>` để inject team profile khi setup dự án

### v2.8.0
- 🚀 **Deployment Wizard** — Triển khai website local lên internet qua Cloudflare Tunnel. Không hosting, không domain, không cấu hình.
  - `--find-port` quét trước, `--serve-cmd` tra cứu theo stack (13 stacks)
  - `--quiet` chế độ agent với output dễ parse
- 🎯 **Triển khai tối ưu token** — Pattern "chuyển sách": devops.md chỉ tới recipe nhỏ gọn khi triển khai
- 📦 **Tech Stack Advisor mở rộng** — 10 danh mục, 56+ công nghệ, 25 combo full-stack

### v2.7.0
- ⚡ **Giao việc Agent song song** — Leader gọi nhiều agent cùng lúc. Xây dựng **nhanh hơn tới 4 lần**.
- 🔍 **Researcher Agent + Tìm kiếm DuckDuckGo** — Tìm kiếm web trực tiếp chỉ dùng Python stdlib (không pip, không API key).
- **Meta Thinker mở rộng** — 45 ngành, 15 framework, 25 archetype, 16 mô hình kiếm tiền, 300+ ý tưởng tính năng
- **18 agent** tổng cộng (trước đó 17)

> 📄 **[Xem nhật ký thay đổi đầy đủ →](CHANGELOG.md)**

---

## ❤️ Ghi Công

Cảm ơn đặc biệt tới **[ui-ux-pro-max-skill](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill)** vì đã tiên phong cách tiếp cận data-driven cho tạo UI/UX.

## 📄 Giấy Phép

MIT © [Nhqvu2005](https://github.com/Nhqvu2005)
